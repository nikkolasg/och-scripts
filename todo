
- make script to arrange table if fields modified in the records table,i.e. create /delete new field, and put default value for others,or reanalyse them

dependency: 	SImpleConfig
		pry
		net/sftp
		activesupport-4.1.6		


sql : select FROM_UNIXTIME(timest,"%k:%i %M %Y") as min from STATS_MSS group by min order by timest

FROM_UNIXTIME : timestamp => date
unix_timestamp : date => timestamp


CDR STATS => 5000-5500 records / cdr
             150-200 cdr / day => MORE MORE MORE
             ==> 750000-1,100000 records / day

TODO
    - database parser requires rotation methods . TODO for cdr & records
    - replace records_fields_file :dump  to automatically dump a file
