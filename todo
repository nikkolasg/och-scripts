
- make script to arrange table if fields modified in the records table,i.e. create /delete new field, and put default value for others,or reanalyse them

dependency: 	SImpleConfig
		pry
		net/sftp
		activesupport-4.1.6		


sql : select FROM_UNIXTIME(timest,"%k:%i %M %Y") as min from STATS_MSS group by min order by timest

FROM_UNIXTIME : timestamp => date
unix_timestamp : date => timestamp


CDR STATS => 5000-5500 records / cdr
             150-200 cdr / day => MORE MORE MORE
             ==> 750000-1,100000 records / day

TODO
    - replace records_fields_file :dump  to automatically dump a file
    
    - when folder is empty ==> empty in db ! 
    - setup --mock /check /fix --mock
    - CHECK ROTATION error cron job !!?? table already exists bla bla bla
    
     

CHECK LIST : create a monitor with unknown source

WHEN NEW SOURCE :
    database dump source #
    database setup source #
    directories setup
    and on rails =)


ANOMALY:
    MOVING AVERAGE : window length = cycle/2  + 1 ==> 7/2 + 1 = 4
    DISCRETIZATION : divide by bins of 10,100,10000 rates


TODOOOOO

5 jours par default

SMS : check Send / Receive dans sms_instats charged 
      rotated table sms_out ==> process in the weekend
TAP SMS : charged party => 1 = MO, 2 = MT


 ## STATS procera
        #nat = 213
        #roam != 213
        #!= pgw out , 228 nat, != 228 roaming

